#include "iostream"
#include "math.h"
#include "cstdlib"
#include "stdio.h"

using namespace std;

double lerPrest();
double calMulta(double prest);
double calJuros(double dias);
double valFinal(double prest, double multa, double juros);
void mostrar(double prest, double multa, double juros, double valfinanl);
void controle();

int main ()
{
  controle();
  return 0;
}
double lerPrest()
{
  double prest;
  cout <<
}

double calMulta(double prest)
{
  return (prest * txmulta);
  
}

double calJuros(double dias)
{
  return (dias*txjuros);
}